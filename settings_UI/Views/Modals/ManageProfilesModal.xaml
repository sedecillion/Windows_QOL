<Window
    x:Class="settings_UI.Views.Modals.ManageProfilesModal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:settings_UI.Views.Modals"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border x:Name="ModalTitleBar" Grid.Row="0" Height="40" Background="Transparent">
            <TextBlock Text="Manage Profiles" VerticalAlignment="Center" Margin="16,0,0,0" Style="{StaticResource CaptionTextBlockStyle}" />
        </Border>

        <Grid Grid.Row="1" Padding="24,10,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="Your Profiles" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Button HorizontalAlignment="Right" Style="{ThemeResource AccentButtonStyle}" Click="AddNewProfile_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="14"/>
                        <TextBlock Text="Add New"/>
                    </StackPanel>
                </Button>
            </Grid>

            <!-- BIND TO THE NEW MODAL PROFILES LIST -->
            <ListView Grid.Row="1" 
                      ItemsSource="{x:Bind ModalProfiles, Mode=OneWay}"
                      SelectionMode="None"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1"
                      CornerRadius="8"
                      Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:ProfileRowUI">
                        <Grid Padding="12,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{x:Bind ProfileName, Mode=OneWay}" Grid.Column="0" VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold" Margin="0,0,12,0"/>
                            
                            <!-- Badges -->
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <Border Background="{ThemeResource SystemFillColorSuccessBrush}" CornerRadius="4" Padding="6,2" Visibility="{x:Bind ActiveBadgeVisibility, Mode=OneWay}">
                                    <TextBlock Text="ACTIVE" FontSize="10" FontWeight="Bold" Foreground="White" />
                                </Border>
                                <Border Background="{ThemeResource SystemFillColorNeutralBrush}" CornerRadius="4" Padding="6,2" Visibility="{x:Bind DisplayedBadgeVisibility, Mode=OneWay}">
                                    <TextBlock Text="VIEWING" FontSize="10" FontWeight="Bold" Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                                </Border>
                            </StackPanel>

                            <!-- Action Buttons -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4">
                                <Button ToolTipService.ToolTip="Switch View" Click="ViewProfile_Click" Background="Transparent" BorderThickness="0">
                                    <FontIcon Glyph="&#xE890;" FontSize="16"/>
                                </Button>
                                
                                <Button ToolTipService.ToolTip="Make Active" Click="ActivateProfile_Click" Background="Transparent" BorderThickness="0">
                                    <FontIcon Glyph="&#xE73E;" FontSize="16" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                                </Button>
                                
                                <Button ToolTipService.ToolTip="Rename" Click="RenameProfile_Click" Background="Transparent" BorderThickness="0">
                                    <FontIcon Glyph="&#xE70F;" FontSize="16"/>
                                </Button>
                                
                                <Button ToolTipService.ToolTip="Delete" Click="DeleteProfile_Click" Background="Transparent" BorderThickness="0">
                                    <FontIcon Glyph="&#xE74D;" FontSize="16" Foreground="Red"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <Border Grid.Row="2" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}" Padding="24,12" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <Button Content="Close" HorizontalAlignment="Right" Width="100" Click="CloseButton_Click" />
        </Border>
    </Grid>
</Window>