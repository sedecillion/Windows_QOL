<Window
    x:Class="settings_UI.Views.Modals.JsonEditorModal"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="ModalTitleBar" Height="32" Grid.Row="0">
            <TextBlock Text="Import / Export Profile" VerticalAlignment="Center" Margin="12,0,0,0" Style="{ThemeResource CaptionTextBlockStyle}"/>
        </Grid>

        <TextBox x:Name="JsonTextBox"
                 Grid.Row="1"
                 Margin="12"
                 AcceptsReturn="True"
                 TextWrapping="NoWrap"
                 FontFamily="Consolas"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch" />

        <TextBlock x:Name="ErrorTextBlock"
                   Grid.Row="2"
                   Margin="12,0,12,0"
                   Foreground="Red"
                   Visibility="Collapsed"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="12">
            <Button Content="Cancel" Click="CancelButton_Click" Margin="0,0,8,0" Width="80"/>
            <Button Content="Save" Style="{ThemeResource AccentButtonStyle}" Click="SaveButton_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>